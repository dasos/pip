# Repository-managed signing configuration.
# release.keystore is deliberately versioned so local and CI release builds
# always use the same signing certificate.
storeFile=release.keystore
storePassword=android
keyAlias=androiddebugkey
keyPassword=android